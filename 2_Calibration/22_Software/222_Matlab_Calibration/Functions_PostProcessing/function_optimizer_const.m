function [c1,c2,c3] = function_optimizer_const(alpha)
% Function for calculating boundary conditions (inequalities)
% not used and is only a place holder for further improvements
%
% Parameter:
%   * alpha: Vector, all calibration values for all 3 axes of a sensor

    c1 = -1;
    c2 = -1; 
    c3 = -1;
end
